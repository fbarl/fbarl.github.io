{
  "name": "fbarl.github.io",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "react-scripts": "1.1.1"
  },
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "styled-components": "^3.1.6"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "rm -rf .cache build && yarn build && gh-pages --dist build --branch master --message 'Deploy to GitHub pages'"
  }
}
